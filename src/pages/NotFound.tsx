import React from "react";
import { MainLayout } from "../layouts";

type NotFoundPropsTypes = {};

const NotFound = (props: NotFoundPropsTypes) => {
  const {} = props;
  return <MainLayout>Not found page</MainLayout>;
};

export default NotFound;
